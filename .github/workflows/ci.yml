name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  python:
    name: Python 3.11
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        # actions/checkout v5.0.1 (Node 24 runtime)
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - name: Set up Python
        # actions/setup-python v6.2.0 (Node 24 runtime)
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.11"
          cache: "pip"
      - name: Install package and dev dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install -e ".[dev]"
      - name: Verify real httpx import
        run: |
          python -c "import httpx; print(httpx.__file__)"
      # RAIKER-2025 — the runtime probe degrades to FTS4 honestly when a build
      # has no FTS5, which is right for a user on an unusual platform and wrong
      # for this repository's own dependency floor. Without this step a wheel
      # that lost FTS5 would leave every test passing and every search silently
      # back on recency ordering, because the fallback is designed not to break
      # anything. So the floor is asserted here, where it can fail loudly.
      - name: Verify the SQLCipher build provides FTS5
        run: |
          python - <<'PY'
          from importlib.metadata import version

          from sqlcipher3 import dbapi2 as sqlcipher

          connection = sqlcipher.connect(":memory:")
          print(
              "sqlcipher3-wheels", version("sqlcipher3-wheels"),
              "| SQLite", sqlcipher.sqlite_version,
          )
          try:
              connection.execute("CREATE VIRTUAL TABLE probe USING fts5(text)")
          except Exception as exc:  # noqa: BLE001 - the message is the report
              raise SystemExit(
                  "This SQLCipher build has no FTS5, so Raiker's text indexes "
                  f"would fall back to FTS4 and recency ordering: {exc}. "
                  "FTS5 arrived in sqlcipher3-wheels 0.5.6; check the floor in "
                  "pyproject.toml."
              ) from None
          print("FTS5: available")
          PY

      - name: Verify SQLCipher memory-security probe
        run: |
          python - <<'PY'
          from pathlib import Path

          from raiker.storage.sqlcipher_probe import probe_memory_security

          result = probe_memory_security(Path.cwd())
          print(
              "SQLCipher memory security:",
              result.reason_code,
              result.sqlcipher_version or "version unavailable",
          )
          if not result.supported:
              raise SystemExit(
                  "The Linux release host could not lock SQLCipher key-bearing pages: "
                  + result.reason_code
              )
          PY
      - name: Run tests
        run: |
          python -m pytest -vv --durations=25
        # The real platform probe above proves Linux page locking separately.
        # Running thousands of isolated stores with locking enabled makes the
        # suite roughly seven times slower and can exhaust the runner's memlock
        # allowance, which hides test progress instead of adding coverage. The
        # BUG-46 tests exercise enabled and fail-closed behavior directly.
        env:
          RAIKER_SQLCIPHER_MEMORY_SECURITY: "off"
      # BUG-205 — the job-wide variable above is what stopped CI from ever seeing
      # a failure that landed on every contributor running the suite the
      # documented way. `cipher_memory_security` is process-global and latches
      # one way, so a test that reads it back depends on what ran before it; the
      # variable meant nothing in the job ever turned it on, and the order
      # dependency was invisible. This step runs the posture file the way a
      # contributor does — same process, variable unset — so the gate sees what
      # they see.
      - name: Run the SQLCipher posture tests without the job-wide override
        run: |
          python -m pytest tests/test_sqlcipher_memory_security.py tests/test_memory_sqlcipher.py -vv
      - name: Run ruff
        run: |
          python -m ruff check .
      - name: Run mypy
        run: |
          python -m mypy raiker apps tests
      - name: Compile sources
        run: |
          python -m compileall -q raiker apps tests

  native:
    # The sandbox runner is the only part of Raiker that is not Python, and it
    # is the part that builds the operating-system boundary. It is built and
    # tested on both platforms that have one, because a boundary that compiles
    # on the author's machine and nowhere else is not a boundary anyone ships.
    name: Native runner (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - name: Checkout
        # actions/checkout v5.0.1 (Node 24 runtime)
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - name: Show toolchain
        working-directory: native
        run: |
          rustc --version
          cargo --version
      - name: Check formatting
        working-directory: native
        run: cargo fmt --check
      - name: Lint
        working-directory: native
        run: cargo clippy --all-targets -- -D warnings
      - name: Test
        working-directory: native
        run: cargo test --all
      - name: Build and install into the package
        run: python scripts/build_native_runner.py
