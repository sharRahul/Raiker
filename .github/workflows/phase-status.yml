name: Phase Status Validation

# TEMPORARILY PAUSED:
# GitHub Actions run limit/quota is exhausted.
# Use local validation gate documented in docs/LOCAL_VALIDATION_GATE.md.
# Restore pull_request/push triggers when Actions quota is available again.
on:
  workflow_dispatch:

jobs:
  phase-status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate phase/status ledger
        run: python scripts/validate_phase_status.py
