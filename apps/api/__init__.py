"""What is left of the old ``apps`` tree: one compatibility shim.

The two Python modules that lived here are :mod:`raiker.app.launcher` and
:mod:`raiker.api.serve`. The web app is a Node project at ``web/``. Nothing new
belongs in this directory — see :mod:`apps.api.launcher` for why the one file
still in it has not been deleted, and when it can be.
"""
