<script lang="ts">
  // The Raiker mark: the governed-core sphere with the Γ_ prompt eye — the
  // same identity as the favicon/app icons (served from the production asset).
  let { size = 28 }: { size?: number } = $props();
</script>

<img src="/favicon.svg" alt="" width={size} height={size} class="logo" />

<style>
  .logo {
    display: block;
  }
</style>
