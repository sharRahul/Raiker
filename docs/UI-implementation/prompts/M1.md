# Prompt — M1: UI Skeleton

You are implementing **Milestone 1** of the Raiker governed-agent UI. Read
`docs/UI-implementation/00_OVERVIEW.md`, `01_INFORMATION_ARCHITECTURE.md`, and `02_SECURITY_UX.md`
first. Develop on branch `claude/raiker-governed-agent-ui-zsqqt6`.

## Goal
Create the `apps/web` Vite + Svelte + TypeScript SPA shell with navigation, the persistent top bar
(`RuntimeStatusBanner` + STOP switch placeholder), and the status-badge system. **No backend calls
yet** — render from clearly-labelled local fixtures. **No claim that any runtime works.**

## Do
- Scaffold `apps/web/` with `package.json`, `vite.config.ts`, `svelte.config.js`, `tsconfig.json`,
  `index.html`, `src/main.ts`, `src/App.svelte`.
- Scripts in `package.json`: `lint` (eslint), `check` (svelte-check/tsc), `test` (vitest),
  `build` (vite build). Keep the JS toolchain isolated from Python tooling.
- Implement: left-nav for all IA sections; `RuntimeStatusBanner` (props-driven, fixture data);
  `StopSwitch` (round red button, confirm dialog, disabled/no-op in M1 with a "wired in M3" note);
  `Badge` component covering every label in `02_SECURITY_UX.md` (text + icon/shape, accessible
  name — never colour-only); routing between sections with empty/loading placeholders.
- `src/fixtures/*.ts` clearly marked as fake sample data.
- Component tests for `Badge` (all variants) and the app shell (nav renders, STOP confirm opens).
- A11y baseline: keyboard nav, visible focus, ARIA labels.

## Don't
- No network calls, no auth, no fake runtime success, no secrets, no policy logic.
- Don't modify Python code or docs truthfulness markers in this milestone.

## Definition of done
`npm --prefix apps/web run lint && npm --prefix apps/web run check && npm --prefix apps/web run test
&& npm --prefix apps/web run build` all pass. Python validation gate unaffected. Commit + push.
