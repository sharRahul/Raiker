# Prompt — M2: Read-only Runtime Dashboard

You are implementing **Milestone 2**. Read `00_OVERVIEW.md`, `01_INFORMATION_ARCHITECTURE.md`,
`02_SECURITY_UX.md`, `03_API_CONTRACTS.md`. Branch `claude/raiker-governed-agent-ui-zsqqt6`.

## Goal
Make the dashboard show **real, read-only** governed state, and add the local auth-token route so
the SPA can call the API. Anything the backend doesn't support shows "unavailable / not
implemented" — never faked.

## Backend (extend `raiker/api/`, reuse services — no raw SQL in routes, no new policy engine)
- `POST /api/auth/session` — mint a local token for the resolved owner principal
  (`resolve_local_principal` + `ApiSessionStore.create_session`); human principals only; reject if
  no owner bootstrapped; never log the token.
- Read routes per `03_API_CONTRACTS.md`: `GET /api/sessions`, `GET /api/sessions/{id}`,
  `GET /api/turns/{id}`, `GET /api/events`, `GET /api/checkpoints[/{id}]`, `GET /api/models`,
  `GET /api/diagnostics`, `GET /api/tasks`.
- Add request/response dataclasses to `raiker/api/schemas.py`; serialize via `serialize_dto`;
  reject unknown request fields.
- Register routers in `raiker/api/app.py`. Add `uvicorn` dep + `apps/api/main.py` (bind
  `127.0.0.1`) so the app is launchable for local dev.

## Frontend
- `src/lib/api.ts`: typed client, Bearer token in memory only (never localStorage).
- Wire existing control reads (`/api/runtime-mode`, `/api/capability-gates`,
  `/api/runtime-readiness`) + the new read routes.
- Build `CapabilityMatrix` (group by domain/tier; exact backend status labels),
  `DisabledCapabilityExplainer`, `EventLogViewer` (filters), `CheckpointViewer`,
  `DiagnosticsPanel`, read-only Runtime Gates & Models views.
- Tier 2–6 / fail-closed caps must render Disabled/Deferred and link "Enable in Security
  Settings →" (panel itself arrives in M5).

## Tests
- Backend per route: 401 without Bearer; happy-path schema; governance preserved (no mutation
  here); events untouched. Contract test for each new schema.
- Frontend: `CapabilityMatrix` renders disabled/deferred correctly from fixtures + a mapping test.

## Don't
Don't enable any capability; don't fabricate data; don't bypass services; don't store secrets.

## Definition of done
Full validation gate green (Python + frontend). Commit + push.
