# Prompt — M7: Tests, Docs & Truthfulness

You are implementing **Milestone 7**. Read every doc in `docs/UI-implementation/` and
`docs/LOCAL_VALIDATION_GATE.md`. Branch `claude/raiker-governed-agent-ui-zsqqt6`.

## Goal
Harden the UI with tests, accessibility checks, and **security regression tests**, then update the
project docs and the truthfulness validator so the repo honestly reflects the new launchable local
web surface — **an alignment, not a loosening.**

## Tests to add/complete
- **Frontend:** component tests (vitest) for each component; contract tests asserting the typed
  client matches backend response schemas; a11y checks (roles/labels/focus).
- **Backend:** endpoint tests for every UI route (auth, schema, governance); contract tests for
  every schema in `raiker/api/schemas.py` used by the UI.
- **Security regression tests (required, must fail if violated):**
  1. UI/API actions cannot bypass policy/`RuntimeAuthority` (denials still fire via API).
  2. Disabled/deferred capabilities display correctly and are not enableable.
  3. Approval resolution stays metadata-only (`executes_action=false`).
  4. Sensitive domains (email/calendar/finance/medical/cctv/home_security/hardware) stay
     blocked/deferred unless backend says otherwise.
  5. STOP only cancels at safe boundary; AI principal cannot interrupt or mutate gates.

## Truthfulness update (alignment, NOT loosening)
- Update: `README.md`, `docs/ARCHITECTURE.md`, `docs/IMPLEMENTATION_STATUS.md`,
  `docs/SECURITY_ARCHITECTURE.md`, `docs/API_AND_CONTRACT_SCHEMAS.md`,
  `docs/GAP_AND_TODO_ANALYSIS.md`, `docs/LOCAL_VALIDATION_GATE.md`.
- Update `scripts/validate_repo_truthfulness.py`: change the launchable-UI marker so it covers
  **both** the plain terminal client **and** the local web dashboard. Keep **every**
  `disabled_deferred` / `metadata_only` / risk marker enforced. Do not remove any disabled-runtime
  marker. Add the new web UI to `docs/ARCHITECTURE.md`'s capability matrix with honest status
  (`implemented_read_only` for read views, `implemented_policy_gated` for governed mutation,
  `metadata_only` where it surfaces metadata-only flows).
- Add the web launch command + smoke checks to `docs/LOCAL_VALIDATION_GATE.md`.
- If any new CLI command surface was introduced (it should not be), update
  `docs/RAIKER_TOOL_AND_PLUGIN_CATALOG.md` accordingly. The deferred `/sessions` **CLI** command
  stays deferred — the API read view is documented separately.

## Definition of done
Full validation gate green, including `scripts/validate_phase_status.py`,
`scripts/validate_repo_truthfulness.py`, and frontend lint/check/test/build. No overclaims.
Commit + push; ensure the PR is updated.
