# BUG-46/48/51/60/64/65/88 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deliver the approved security, onboarding, policy, runtime-disclosure, task-intent, transcript-export, and loopback-rate-limit fixes as one verified release.

**Architecture:** Implement each independent subsystem from its linked plan, then run the shared live-provider, browser, packaging, documentation, and CI acceptance plan. Preserve Raiker's local-first authority boundary: the runtime owns security and execution state, while the UI renders and controls that state.

**Tech Stack:** Python 3.11+, FastAPI, SQLCipher, Svelte 5, TypeScript, Vitest, Playwright, PyInstaller, pystray, GitHub Actions.

## Global Constraints

- Never persist, print, commit, or place provider credentials in command history. Enter live-test credentials only through the authenticated UI and remove them through the UI after testing.
- Keep public-bind authentication, rate limiting, approvals, owner scoping, and redaction fail-closed.
- Do not run `PRAGMA cipher_memory_security = ON` in the resident Windows process until the isolated probe reports success.
- Do not create fake setup completion, backup, tray, citation, or task execution state.
- Use the approved design in `docs/superpowers/specs/BUG_46_48_51_60_64_65_88_DESIGN.md` as the source of truth.

---

### Task 1: SQLCipher memory-security posture (BUG-46)

- [x] Execute `docs/superpowers/plans/2026-08-11-sqlcipher-memory-security.md` completely.
- [x] Commit only that subsystem with `git commit -m "fix: probe SQLCipher memory security safely"`.

### Task 2: First-run setup and native tray (BUG-48)

- [x] Execute `docs/superpowers/plans/2026-08-11-setup-and-native-tray.md` completely.
- [x] Commit only that subsystem with `git commit -m "feat: add first-run setup and native tray"`.

### Task 3: Static policy and runtime refusal disclosure (BUG-51, BUG-59, BUG-60)

- [x] Execute `docs/superpowers/plans/2026-08-11-policy-and-runtime-disclosure.md` completely.
- [x] Commit only that subsystem with `git commit -m "fix: align policy and runtime refusal disclosure"`.

### Task 4: Explicit task execution intent (BUG-64)

- [x] Execute `docs/superpowers/plans/2026-08-11-task-execution-intent.md` completely.
- [x] Commit only that subsystem with `git commit -m "fix: separate task approval from execution"`.

### Task 5: Self-contained transcript exports (BUG-65)

- [x] Execute `docs/superpowers/plans/2026-08-11-transcript-citation-export.md` completely.
- [x] Commit only that subsystem with `git commit -m "fix: make transcript citations self contained"`.

### Task 6: Loopback-aware API rate limiting (BUG-88)

- [x] Execute `docs/superpowers/plans/2026-08-11-loopback-rate-limits.md` completely.
- [x] Commit only that subsystem with `git commit -m "fix: exempt safe loopback reads from rate limits"`.

### Task 7: Release acceptance and publication

- [ ] Execute `docs/superpowers/plans/2026-08-11-release-acceptance.md` completely.
- [ ] Commit documentation and acceptance-only changes with `git commit -m "docs: record bug acceptance evidence"`.
- [ ] Push the accumulated commits to `origin/main`, monitor every required GitHub Actions check, diagnose any failure from its logs, fix it with a focused commit, and repeat until all required checks are green.
