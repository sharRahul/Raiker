{
  "name": "raiker-root",
  "private": true,
  "version": "0.0.0",
  "description": "Raiker governed-agent runtime. Root package.json delegates web-app (apps/web) scripts so `npm run <script>` works from the repo root. The web app is the only JS project; this file has no dependencies of its own and does not affect the Python package or its ruff/mypy/pytest gate.",
  "scripts": {
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run preview",
    "check": "npm --prefix apps/web run check",
    "lint": "npm --prefix apps/web run lint",
    "test": "npm --prefix apps/web run test"
  }
}
