{
  "name": "raiker-root",
  "private": true,
  "license": "Apache-2.0",
  "version": "0.0.0",
  "description": "Raiker governed-agent runtime. Root package.json delegates web-app (web) scripts so `npm run <script>` works from the repo root. The web app is the only JS project; this file has no dependencies of its own and does not affect the Python package or its ruff/mypy/pytest gate.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "check": "npm --prefix web run check",
    "lint": "npm --prefix web run lint",
    "test": "npm --prefix web run test",
    "test:e2e:mocked": "npm --prefix web run test:e2e:mocked",
    "test:e2e:live": "npm --prefix web run test:e2e:live"
  }
}
